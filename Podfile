# Uncomment this line to define a global platform for your project
platform :ios, '9.0'

target '777Club' do
    use_frameworks!
  
  # Pods for 777Club
  pod 'Firebase'
  pod 'Firebase/Database'
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  
  target '777ClubTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target '777ClubUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
